module.exports = {
  darkMode: 'class', // or 'media' if you prefer automatic dark based on system
  theme: {
    extend: {
      fontFamily: {
        sans: ['Manrope', 'sans-serif'],
      },
    },
  },
  plugins: [],
}
